[package]
name = "plato-tile-ranker"
version = "0.1.0"
edition = "2021"
description = "High-performance tile ranking engine — zero-allocation scoring, weighted multi-factor sort, top-k selection"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
