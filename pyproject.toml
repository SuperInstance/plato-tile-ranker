[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-ranker"
version = "0.1.0"
description = "Multi-signal tile ranking with keyword gating and deadband boost"
license = {text = "MIT"}
requires-python = ">=3.8"
